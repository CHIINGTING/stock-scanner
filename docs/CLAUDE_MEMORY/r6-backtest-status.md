---
name: r6-backtest-status
description: "R6 pullback/crash-low backtest — DOCS PHASE DONE (2a/2b/2c/2d/R6-3 + interpretation + R6-4 proposal) + R6-5 fresh-cache reproducibility rerun DONE & STABLE (no pause); next = cross-regime validation (NOT yet done); baseline stop over-stops"
metadata: 
  node_type: memory
  type: project
  originSessionId: 19003089-9b50-4abc-b28a-075d50a8075a
---

R6 = pullback / crash-low entry **backtest** (decision-support only; never orders / live ranking). Lives in `internal/r6backtest/` + `cmd/r6backtest/`, a standalone package that imports the scanner's EXPORTED signal APIs read-only (CalculateRSRanks / ComputeVCP / ComputeMomentum / ComputeMultiTimeframe). Runs off the local `.cache` (no Yahoo). Output → gitignored `reports/backtest_pullback_*.csv` + `_summary_*.md`.

Phases:
- **R6-1** (done): plan + throwaway prototype. Data = ~1973 cached stocks, 2024-06→2026-06 (~485 bars).
- **R6-2a** (done, commit 3d3b7fa): framework — Universe loader, as-of RS panel (real CalculateRSRanks, ~200ms/364 dates), engine (entry=i+1 open, forward/stop/drawdown, cooldown dedup), CSV/markdown schema, tests.
- **R6-2b** (done, commit d79b622): Setup A (A_MA20_PULLBACK / A_MA60_PULLBACK) + Setup B (B_PULLBACK_5/8/10/15/20). **Returns are stop-adjusted (main) with hold-to-horizon as comparison**; CSV has return_* and hold_return_* + stop_date/stop_price. Cooldown=10 bars, resets on a fresh 20-day high. Setup names/buckets are stable. forbidden tokens (BUY/AUTO_BUY/PLACE_ORDER) guarded by test.
- **R6-2c** (done, commit a266f48): Setup C real VCP retest (C_VCP_MA20/MA60/BASE_LOW_RETEST) + grade/quality grouping summary.
- **R6-3** (done, commits 86c6689/80eab5e): stop-policy benchmark (`-stopbench`) + interpretation doc.
- **R6-2d** (done, uncommitted as of 2026-06-08): Setup D crash-regime survivor CASE STUDY. New files `regime.go` (0050 proxy 20d≤−8% regime detection, event segmentation w/ 5-bar merge gap, breadth-below-MA20 context), `setup_d.go`, `setup_d_test.go`; + CrashContext type, crash CSV/summary writers, main.go wiring. Always LOW confidence (ForceLowConfidence). Main = RS≥70 + relative_return_vs_market_20d ≥+5pp + weekly≠DOWNTREND + flow≠SHIFT_DOWN + near-MA20 + vol-dry. Plus HIGH_RS vs LOW_RS cohort split (relaxed: regime + near-MA20 only). Tests pass; verified run: event_count=3 (4 detected, 1 pre-warmup), range 2025-03-13~2026-03-31, 537 survivor trades, avg market −9.4% / survivors +14pp relative. HIGH_RS more resilient on relative basis (+6.9pp vs −0.4pp). Outputs → reports/backtest_crash_survivors_*.

**Key finding (R6-2b run, cache data):** baseline stop (`BREAK_MA60` + `PCT_-10`) over-stops — stop_hit_rate 77–93% (Setup D even higher, 90–98%), stop_saved_or_hurt_delta is NEGATIVE at every horizon (B_20 60d ≈ −23%). Holding beats stopping with these params. This is a backtest finding, NOT an impl bug; do not "fix" by quietly changing stops.

Setup conditions/params baseline mirror configs/config.yaml (see [[scanner-enhancement-plan]] R5 calibration ethos); final params must be validated on our own TW data, not adopted as-is.

---

## R6 Phase Done Summary (as of 2026-06-08)

**R6 first phase complete & committed** (3d3b7fa → d434c2d). Commit chain: R6-2a framework+RS panel → R6-2b Setup A/B (stop-adjusted) → R6-3 stop policy benchmark (86c6689) → R6-3 interpretation docs (80eab5e) → R6-2c Setup C real VCP (a266f48) → R6-2d Setup D crash-regime survivor case study (2687d58) → R6 final interpretation docs (16ed741) → R6-4 stop profile proposal docs (d434c2d). Interpretation docs: `docs/SPEC_R6_BACKTEST_INTERPRETATION.md`, `docs/SPEC_R6_3_STOP_BENCHMARK_INTERPRETATION.md`, `docs/SPEC_R6_4_STOP_PROFILE_PROPOSAL.md`. All docs-only — no live scanner / config default / report change.

**Key conclusions:** A_MA20 > A_MA60; B deep pullback 15–20% has stronger hold return (but depends on not getting washed out); C_VCP_MA20 has enough samples but BASELINE stop is too strict for VCP retest (washes out in normal contraction); D shows HIGH_RS relatively crash-resistant BUT event_count tiny → confidence always LOW (case study, not extrapolatable); BASELINE stop too strict (over-stops); ATR_3 = current most robust candidate; PCT_15 = simplified candidate; NO_STOP highest return but deepest tail drawdown.

**Why NOT change live scanner now:** data window 2024-06→2026-06 is bullish/recovery-biased (structurally favors NO_STOP/wide stops — not extrapolatable); not yet validated on fresh / cross-regime data; ran off read-only `.cache`; D is a case study, can't extrapolate; R6 is offline backtest/strategy research, NOT an auto-trading system — no orders, no broker, never trades for the user. BASELINE stays; ATR_3/PCT_15 are candidates only.

**Next steps (recommended order):** (1) **PREFERRED — pause dev, start using A/B report + R6 conclusions to observe** (lowest cost, validates conclusions on real watchlist; also fills the still-missing fresh-data observation in [[scanner-ab-guardrail-status]]). (2) If continuing engineering: build a report DISPLAY layer surfacing backtest insight, but do NOT change strategy. (3) If pushing toward finalization: do R6-5 fresh data rerun / cross-regime validation FIRST (prerequisite for any stop profile adoption). (4) R6-4 stop profile impl draft is LEAST urgent — must wait for fresh / cross-regime validation before considering.

**R6-5 fresh data rerun DONE (2026-06-08, commit c6c0f45 — doc `docs/SPEC_R6_5_FRESH_DATA_RERUN.md`).** Positioned as **independent fresh-cache reproducibility check, NOT cross-regime validation** (because `.cache` was already 2026-06-08 latest). Method: re-fetched fresh into gitignored `.cache_fresh_r6_5/` via existing scanner + a `/tmp` config (cache_dir override, never tracked); `.cache` untouched; proxy `0050` (ETF, absent from common-stock market scan) independently re-fetched so Setup D could run. OLD baseline re-run from `.cache` → `reports/r6_5_baseline_old/`; FRESH → `reports/r6_5_fresh/` (both gitignored). **Result: STABLE, NO PAUSE.** CSV-level: A/B/C 26334 trades 0 numeric diff (only 20 stock_name strings differ); stop benchmark 90 rows byte-identical; Setup D 0 diff except non-gating `breadth_below_ma20_pct` ≤0.06pp (universe 1974→1967) + LOW_RS cohort n 3530→3519. All R6 conclusions reproduced: A_MA20>A_MA60, B deep 15–20% stronger hold, C_VCP_MA20 n=2592 ample, D HIGH_RS +6.9pp vs LOW −0.4pp (conf LOW), BASELINE strictest / ATR_3 most robust / PCT_15 simple / NO_STOP highest-return+deepest-dd. Two unrelated dirty files (configs/sectors.yaml, stocks.yaml) were committed separately first (2315719) per user before R6-5; `.gitignore` got `.cache_fresh_r6_5/` (commit before doc). go build/vet/test all PASS; forbidden tokens none.

**R6-6 = Recent Bull Regime Validation — DONE & COMMITTED (commit 9da5bb4); build/vet/test green, all 13 spec fields present.** Note: committed doc/output anchored to cache axis end 2026-06-08 (cutoff 2026-05-11), but `.cache` has since drifted to 2026-06-10 → re-running shifts numbers; treat committed result as a point-in-time snapshot, don't re-run unless a refresh is explicitly wanted. (orig 2026-06-09 verify below.) New files `internal/r6backtest/recentbull.go` (+`_output.go`,+`_test.go`) + `-recentbull` flag in cmd/r6backtest/main.go + doc `docs/SPEC_R6_6_RECENT_BULL_REGIME_VALIDATION.md`; reports → gitignored `reports/r6_6_recent_bull/`. Ran read-only on `.cache` (1974 stocks, axis end 2026-06-08, 20d cutoff 2026-05-11). RESULT (20d primary, stop=BASELINE, recent_4m primary / 6m context): Q1 A_MA20>>A_MA60 (4m 5.7% vs 0.8%) ✓; Q2 deep B 15-20% too slow (B_20 only 332 signals vs B_5 3272) ✓; Q3 shallow 5-10% better for super-bull (more signals, comparable 20d) ✓; Q4 **C_VCP_MA20 does NOT beat plain A_MA20 in recent bull** (4m 4.0% vs 5.7%) — DIVERGES from full-2y R6; Q5 C_VCP_BASE_LOW too slow/thin (recent_2m INSUFFICIENT avail=8); Q6 ATR_3 still best stopped compromise ✓; Q7 PCT_15 ok/簡化備案 ✓; Q8 NO_STOP highest 20d + deepest rdd_p90 (tail real, near-term UNDERSTATED by truncation) ✓; Q9 BASELINE over-strict, even more pronounced in bull ✓. Only non-OK cells = C_VCP_BASE_LOW @ recent_2m (avail_20d=8, all 9 policies). Per-window totals: signal 2m6136/4m10912/6m15718, unmatured constant 2454. go build/vet/test all PASS; forbidden tokens none. R6-4 stop-profile: NO promotion, NO pause — recent-bull can't promote; cross-regime still the prerequisite & still NOT done.

**(superseded) R6-6 original cross-regime/10y plan:** User PIVOTED away from cross-regime/10y: R6-6 validates which setup/stop profile fits the CURRENT super-bull environment only — NOT cross-regime. Output titled "Current Bull Regime Verdict"; must state it only applies to recent strong-bull, cannot extrapolate to bear/sideways/crash, cannot change live scanner/config/stop-profile defaults. Windows by SignalDate, nested: recent_2m (overlay) ⊂ recent_4m (PRIMARY) ⊂ recent_6m (context), anchored to data axis end. NO re-fetch — existing `.cache` (2024-06→2026-06) already covers last 6m; reports → gitignored `reports/r6_6_recent_bull/`. Setup D out of scope (no crash in bull window). Reuse collectEntries/buildTrade/ComputeStats via a new `-recentbull` flag + new file; do NOT change engine/Trade struct/defaults.

**User's horizon judgment rule (DURABLE preference, applies beyond R6-6):** PRIMARY horizon = **20d**, NOT 5d/10d. Rationale: TW strong-stock rhythm this year = 強勢→處置→下跌/冷卻→出處置→再攻擊, the 處置(~10d)+再處置/出處置 cycle ≈ 20 trading days. 5d/10d = early-reaction / path observation only; 60d = optional reference, never the main verdict for recent-bull. **20d maturity rule:** signals triggered within the last 20 trading days = `UNMATURED_20D`, MUST be excluded from 20d stats; 20d stats only use `signal_date <= last_available_date − 20 trading days`. Per cell output: `matured_20d_count`, `unmatured_20d_count`, `available_20d`, `status` where status = OK (available_20d≥30) / LOW_SAMPLE (12–29) / INSUFFICIENT (<12). If recent_2m 20d-matured too few, keep numbers but base verdict on recent_4m/6m 20d.

**R6-7 = Report Holding Horizon Hint — IMPLEMENTED & VERIFIED (uncommitted as of 2026-06-11; plan doc committed 9d615cb).** New `internal/scanner/horizonhint.go` (+`_test.go`, 7 tests pass) + `WatchlistEntry.HorizonHint *HoldingHorizonHint` + Config `EnableHorizonHint yaml:"show_horizon_hint"` (default false) + report ⑧ block (gated by non-nil+Computed) + `intsSlash` template func + config.yaml `show_horizon_hint: false`. build/vet/test all green. Verified live: flag OFF → 0 ⑧ in report (unchanged); flag ON → ⑧ on every card. `--no-market` shows DEFAULT (RS table nil without market scan → C/B can't fire); run-all needed for C/B matches. index.html (tracked report artifact) deliberately kept OUT of the change set. Matcher thresholds are v1 module constants (待校準): VCP quality≥70, RS≥70, NewHighScore≥60, dist≥-25, pullback 5–20% → nearest of {5,8,10,15,20}, near-MA ≤3%. **Plan/design below was the pre-impl spec:** Display-only ⑧ report card: 主要觀察週期=20d (5/10 early reaction, 60 ref), backed by R6-6 recent-bull. Setup-match priority C(VCP)→B(52w-high pullback)→A(MA20/MA60 pullback)→D(crash survivor, v1 skip)→Default(still show 20d). Red lines: no score/sort/WatchAction/ExplosionProb/stop-profile change, no auto-trade; report copy must say 觀察週期 not 買/賣/持有到第N天. config `show_holding_horizon: false` default. **COLLISION: `WatchlistEntry.HoldingHorizon` already taken by R7-1 (`*HoldingHorizonResult`, stage+ATR shadow in holdinghorizon.go, merged PR #2, not in report) — R6-7's `HoldingHorizonHint` needs a different field (recommend `HorizonHint`). R6-7 (setup+backtest, report) vs R7-1 (stage+ATR, shadow) = two parallel horizon notions; relationship is an open decision.** Input map: C→Shadow.VCP.{Valid,QualityScore>=70}+Shadow.RS.RSRankPercentile>=70+Shadow.Momentum.Flow!=StructuralShiftDown; B→Shadow.NewHigh.{DistanceFrom52wHighPct>=-25,NewHighScore}+RS; A→A.MA20/A.Close + MA60 computed from candles (not on StockAnalysis). All Shadow.* nil unless their enable flag on → matchers degrade to Default when off.

**Status: R6-6 DONE & committed; R6-5 reproducibility STABLE. R6-7 = plan only, not started. Next (separate track) = TRUE cross-regime validation (more bear/crash periods) — still NOT done; remains prerequisite before any stop-profile adoption. stop profile defaults UNCHANGED. Do NOT auto-start coding — wait for explicit go.**
